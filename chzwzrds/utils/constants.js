const bn = require('bn.js');

module.exports = {
  STARTING_COST: new bn('70000000000000000'),
  COST_INCREMEMENT: 130,
  EXCLUSIVE_COUNT: 1234,
  SALE_DURATION: 47615,
  NEUTRAL_WIZARD_COST: new bn('70000000000000000'),
  POWER_SCALE: new bn('1000'),
  ELEMENTAL_WIZARD_COST: new bn('70000000000000000'),
  ELEMENTAL_WIZARD_INCREMENT: 1000,
  TENTH_BASIS_POINTS: 100000,
  ADMISSION_DURATION: 30, // > 0;
  // REVIVAL_DURATION % (ASCENSION_DURATION + FIGHT_DURATION + DUEL_TIMEOUT_DURATION + CULLING_DURATION) == 0;
  REVIVAL_DURATION: 170, // > 0;
  ASCENSION_DURATION: 30, // > 0
  FIGHT_DURATION: 100, // > 0; FIGHT_DURATION >= DUEL_TIMEOUT_DURATION * 2
  CULLING_DURATION: 20, // > 0; CULLING_DURATION >= DUEL_TIMEOUT_DURATION
  BLUE_MOLD_BASE_POWER: 50, // > 0;
  SESSIONS_BETWEEN_MOLD_DOUBLIN: 5, // > 0;
  DUEL_TIMEOUT_DURATION: 20, // >= 20
};

// TODO: check constants
